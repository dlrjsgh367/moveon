spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/auth
    username: root
    password: ${MYSQL_ROOT_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
server:
  port: 8080
jwt:
  secret: ${JWT_SECRET}
  ttl-seconds: ${JWT_TTL_SECONDS:3600}
